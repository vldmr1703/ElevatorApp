package com.lnu.designing.floor.incoming.strategy.impl;

import com.lnu.designing.elevator.Elevator;
import com.lnu.designing.floor.incoming.strategy.IncomingStrategy;
import com.lnu.designing.person.Person;

import java.util.List;

public class RandomIncomingStrategy implements IncomingStrategy {
    @Override
    public void reorderQueue(List<Person> personList, Elevator elevator) {//on elevator arrive
//        elevator.
//        personList.stream().findFirst().get().getFloorIdToGo()

    }
}
